#pragma once

namespace kke {
/**
 * @brief Encoded texture file type supported by upload helpers.
 */
enum class TextureType {
	/**
	 * @brief Portable Network Graphics image data.
	 */
	PNG,

	/**
	 * @brief JPEG image data.
	 */
	JPEG,
};
}	// namespace kke
